---
layout: null
permalink: /sitemap.xml
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% assign allowed = "/about/,/research/,/cv/" | split: "," %}
{% assign items = site.pages | concat: site.documents %}
{% for item in items %}
  {% assign keep = false %}
  {% for prefix in allowed %}
    {% assign plen = prefix | size %}
    {% assign head = item.url | slice: 0, plen %}
    {% if head == prefix %}{% assign keep = true %}{% endif %}
  {% endfor %}
  {% if keep %}
  <url>
    <loc>{{ item.url | absolute_url }}</loc>
    {% if item.last_modified_at %}<lastmod>{{ item.last_modified_at | date_to_xmlschema }}</lastmod>{% elsif item.date %}<lastmod>{{ item.date | date_to_xmlschema }}</lastmod>{% endif %}
  </url>
  {% endif %}
{% endfor %}
</urlset>
